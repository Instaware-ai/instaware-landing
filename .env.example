# InstaWare Landing Page — Environment Variables
# Copy to .env.local and fill in values

# API Backend URL (optional — leave empty to run in standalone mode)
# Set this once your Rust backend is deployed
# Example: https://api.instaware.ai
NEXT_PUBLIC_API_URL=
